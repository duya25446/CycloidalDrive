# 摆线减速器脚本中文翻译对照

本文件为界面英文文案的中文对照，便于理解参数含义。

## 1. 命令与分组
- create cyclo reducer → 创建摆线减速器
- Necessary param → 必要参数
- Optionary param → 可选参数
- Cycloidal gear centor hole → 摆线轮中心孔
- Cycloidal gear to output disk → 摆线轮与输出盘
- Set about → 设置对象

## 2. 必要参数
- Raducation ratio → 减速比
- Eccentric amount → 偏心量
- Ring pin diameter → 外针直径
- Ring pin pitch diameter → 外针节圆直径
- Cycloidal curve plot num par thooth → 每齿采样点数
- minimum Pressure angle → 最小压力角

## 3. 可选参数
- Draw centor hole → 绘制中心孔
- Diameter → 直径
- Draw around hole → 绘制周孔
- Draw output disk pin → 绘制输出盘销
- Cycloidal gear hole → 摆线轮孔
- Output disk pin → 输出盘销
- Hole num → 孔数量
- Hole diameter → 孔直径
- Centor to hole distance → 中心到孔距
- Pin num → 销数量
- Pin diameter → 销直径
- Centor to pin distance → 中心到销距

## 4. 草图与组件命名
- Cycloidal reducer → 摆线减速器
- Cycloidal gear → 摆线轮
- Ring pins → 外针
- Output disk pin → 输出盘销
